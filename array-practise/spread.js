let numbers = [9, 3, 5, 4, 1, 2, 8];
let max = Math.max(...numbers);
console.log(max);

let username = "BooleanTron";
let letters = [...username];
console.log(letters);


//the SPREAD OPERATOR unpack any array or string - to use it just preceed your array or string with 3 dots like (...numbers)
